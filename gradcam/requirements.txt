# Core dependencies for the gradcam toolkit module.
#
# Install with:
#     pip install -r requirements.txt
#
# For CPU-only PyTorch, use the official index command instead:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

torch>=1.10.0
torchvision>=0.11.0
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=8.0.0
